syntax = "proto3";
package Protocol; // Protocol이라는 namespace 이름을 지어준 것

enum ActorType
{
	PLAYER_TYPE_NONE = 0;
	PLAYER_TYPE_KNIGHT = 1;
	PLAYER_TYPE_MAGE = 2;

	MONSTER_TYPE_NONE = 3;
	MONSTER_TYPE_KNIGHT = 4;
	MONSTER_TYPE_MAGE = 5;
}

enum MoveState
{
	MOVE_STATE_NONE = 0;
	MOVE_STATE_IDLE = 1;
	MOVE_STATE_MOVE = 2;
	MOVE_STATE_JUMP = 3;
}